<div class="grid content-between h-screen">
  <div>
    <%= render '/shared/user_navbar'%>
    <%@entities.each do |entity|%>
    <div class="flex gap-2 shadow-xl p-4 w-full">
            <div class="avatar">
              <div class="w-24 mask mask-squircle">
                <img src="<%= entity.name %>" />
              </div>
            </div>
            <div class="grid content-between">
              <p class=""><%= entity.name %></p>
              <span class="text-sm"><%= entity.created_at.strftime("%b %d, %Y") %></span>
            </div>
            <div>
              <p class="font-bold text-xl">$17.00</p>
            </div>
          </div>
    <%end%>
  </div>
  <div class="flex flex-col text-center justify-end bg-[#5FB523] p-3">
    <%= link_to 'ADD TRANSANCTION', new_category_entity_path, class:"border border-xl border-white text-white px-8"%>
  </div>
</div>